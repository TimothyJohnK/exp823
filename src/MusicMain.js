import React, { component } from "react";

export default class MusicMain extends Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
  }
  render() {
    return <div />;
  }
}
